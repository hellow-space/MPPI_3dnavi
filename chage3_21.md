# 今日 Nav2 参数修改汇总

## 1. 彻底关闭横向运动（禁止横移）
**修改内容**：
```yaml
vy_std: 0.001
vy_max: 0.001
vy_min: -0.001
```
**修改原因**：
- 业务需求：不需要横移功能，避免路径绕弯、漂移。
- 技术约束：不能设为 0，否则会触发 `NaN` 速度报错，故用极小值模拟“无横移”效果。
- 行为效果：机器人行为接近差速轮，仅支持前进/后退/转向。

---

## 2. 放开原地自转能力（姿态调整核心）
**修改内容**：
```yaml
wz_std: 0.4
wz_max: 2.5
wz_min: -2.5
```
**修改原因**：
- 硬件能力：机器人支持原地 0 半径自转。
- 问题根源：之前 `wz_std` 过小，MPPI 不会采样原地旋转动作。
- 行为效果：快到终点姿态垂直时，机器人可原地转向对准路径。

---

## 3. 关闭 `PreferForwardCritic`（解除自转限制）
**修改内容**：
```yaml
PreferForwardCritic:
  enabled: false
```
**修改原因**：
- 该评价器会强制机器人“优先前进”，惩罚原地自转动作，是导致垂直卡死的元凶。
- 行为效果：允许机器人选择“先自转调整姿态，再前进”的脱困策略。

---

## 4. 调整 MPPI 温度参数（提升轨迹探索性）
**修改内容**：
```yaml
temperature: 0.15
```
**修改原因**：
- 温度过低（如 0.1）会让 MPPI 过于保守，不敢选择“先自转、再前进”这种短期代价更高的轨迹。
- 行为效果：适度提升温度后，MPPI 会主动探索并选择原地自转的脱困轨迹。

---

## 5. 保持最小角速度门槛（保证自转生效）
**修改内容**：
```yaml
min_theta_velocity_threshold: 0.001
```
**修改原因**：
- 该参数定义“最小生效角速度”，小于此值会被判定为无动作。
- 保持 0.001 是为了让微小自转也能被识别并输出，避免“想转转不动”。
- 纠正认知：**不能改为 0.05**，否则会提高自转门槛，导致更难启动旋转。

---

## 6. 确认全向运动模型（底层支持自转）
**修改内容**：
```yaml
motion_model: "Omni"
robot_model_type: "nav2_amcl::OmniMotionModel"
```
**修改原因**：
- 全向模型（Omni）天然支持原地自转，与硬件能力匹配。
- 遗留的 `AckermannConstrains` 对 Omni 模型无效，不影响旋转。

---

## 7. 关闭 `TwirlingCritic`（不限制旋转动作）
**修改内容**：
```yaml
TwirlingCritic:
  enabled: false
```
**修改原因**：
- 避免对原地自转动作施加额外惩罚，保证转向自由。

---

## 8. 强化路径跟随权重（保证直线跟线）
**修改内容**：
```yaml
PathFollowCritic:
  cost_weight: 30.0
```
**修改原因**：
- 强制机器人严格贴合全局路径，避免偏移、绕路，保证直线行走。

---

## 9. 终点角度容差设置（避免过度严苛）
**修改内容**：
```yaml
precise_goal_checker:
  yaw_goal_tolerance: 0.1
```
**修改原因**：
- 放宽终点角度要求，避免机器人因角度微小偏差而卡死在终点前。

---

## 10. 障碍物惩罚配置（避免不敢动）
**修改内容**：
```yaml
ObstaclesCritic:
  repulsion_weight: 5.0
  critical_weight: 20.0
```
**修改原因**：
- 平衡避障与运动灵活性，避免机器人因轻微路径偏差就不敢移动、不敢转向。

---

### 整体目标
通过以上修改，机器人实现：
1.  **无横移**：行为接近差速轮，稳定走直线。
2.  **可原地自转**：快到终点姿态垂直时，能自主转向对准。
3.  **不绕路、不漂移**：严格跟随全局路径。
4.  **不卡死**：垂直姿态下可脱困，顺利到达终点。

---

